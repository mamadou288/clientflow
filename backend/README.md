# ClientFlow — Backend (Django REST)

API REST du CRM ClientFlow. **En construction**, app par app.

## Stack visée
Django · Django REST Framework · PostgreSQL · JWT · pytest · Docker · CI.

## Apps prévues
- `crm/` — companies, contacts, deals (entités couplées).
- `users/` — authentification / comptes (domaine isolé).

> Détails de construction : suivis leçon par leçon dans `../../notes/backend/`.
